# ``XestiNetwork``

@Metadata {
    @PageColor(blue)
}

A simple network abstraction layer.

## Overview

The XestiNetwork framework provides a simple network abstraction layer for
Swift.

Inspired by Moya, but without the nonsense (and without the cruft of Alamofire).
